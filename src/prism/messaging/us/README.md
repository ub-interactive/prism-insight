# messaging/us

Redis and GCP Pub/Sub signal publishers currently live in `messaging/shared/` and are oriented toward US trading workflows.

Add US-specific messaging adapters here if signal routing diverges from shared publishers.
